node_modules/
dist/
dist-ssr/
*.local

# Capacitor native projects are generated by `npx cap add android`
# and should stay OUT of version control to keep the repo lightweight.
# Remove these two lines if you want to commit the native project as-is.
android/
ios/

# Editors / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
*.log
Thumbs.db
